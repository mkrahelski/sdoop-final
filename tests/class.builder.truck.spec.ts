import { expect } from "chai";
import "mocha";

describe("Test Review Framework", () => {
    it("should be 1 == 1", () => {
        expect(1).to.equal(1);
    });
});

//since mixons are part of this one. This is probably tested fundumentally here.